{{yield (hash
  menu=(component 'data-table-menu' selection=selection data-table=this)
  content=(component 'data-table-content' rows=rows noDataMessage=noDataMessage enableSelection=enableSelection fields=parsedFields)
  )
  this}}
