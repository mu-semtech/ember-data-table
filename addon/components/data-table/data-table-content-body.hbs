{{yield (hash
    isLoading=@data-table.isLoading
    content=@content
    offset=this.offset
    wrappedItems=this.wrappedItems
    enableLineNumbers=@enableLineNumbers
    hasClickRowAction=(and @onClickRow true)
    onClickRow=this.onClickRow
    toggleSelected=this.updateSelection
    selection=@data-table.selection
    enableSelection=@enableSelection
    noDataMessage=@noDataMessage
    DataCells=(component "data-table/default-data-table-content-body" data-table=@data-table))}}
