import { paginationIcon } from 'dummy/helpers/pagination-icon';
import { module, test } from 'qunit';

module('Unit | Helper | pagination icon');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = paginationIcon([42]);
  assert.ok(result);
});
