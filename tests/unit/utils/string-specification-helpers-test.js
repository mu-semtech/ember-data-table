import stringSpecificationHelpers from 'dummy/utils/string-specification-helpers';
import { module, test } from 'qunit';

module('Unit | Utility | string-specification-helpers', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = stringSpecificationHelpers();
    assert.ok(result);
  });
});
